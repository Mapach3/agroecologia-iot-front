export enum URLs {
  ROOT = "/",
  LOGIN = "/login",
  LOGOUT = "/logout",

  //Business
  USERS = "/usuarios",
  ROLES = "/roles",
  GARDENS = "/huertas",
  CROPS = "/cultivos",
  PROFILE = "/perfil",

  // Common routes
  DETAIL = "/:id/detalle",
  NEW = "/nuevo",

  SHARED_GARDENS = "/huertas-compartidas",
}
