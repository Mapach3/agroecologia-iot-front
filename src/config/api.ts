export enum API {
  //Administrative
  LOGIN = "/auth/login",
  LOGOUT = "/auth/logout",
  REFRESH_TOKEN = "/auth/refreshtoken",

  //Business
  USERS = "/users",
  ROLES = "/roles",
  GARDENS = "/gardens",
  CROPS = "/crops",
}
